# Security

Do not include API keys, Telegram bot tokens, chat IDs, `.env`, or local database files in issues, pull requests, screenshots, or commits.

If a secret is accidentally committed, revoke it immediately before removing it from Git history.
