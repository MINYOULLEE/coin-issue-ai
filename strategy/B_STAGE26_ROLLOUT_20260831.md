# Stage26 운영 전환 상태 — 2026-08-31

**아래는 초기 전환 이력이다. 사용자 추가 승인 이후 설정·텔레그램·웹 완료 상태는 `B_STAGE26_APPROVED_20260831.md`를 우선한다.**

## 완료

- B 운영 전략 ID: `b_core_sparse_stage26`. 기존 Stage16 core + ALGO/ETH/VET 보조 조합.
- Supabase `plan-b-strategy` v7, `plan-b-executor` v14, `plan-b-account-read` v14 배포. 원격의 의존 파일 포함 19개 파일을 로컬 소스와 정규화 대조했다.
- DB 기회 상태·시간별 판정 기록·종목별 규칙·원자적 신호 발행 및 그룹 배제 예약 적용. 테스트 트랜잭션은 전부 롤백했고 실주문을 강제로 만들지 않았다.
- 이전 연구에서 초기 상태를 재현하고 96시간+3시간을 복구하여 UTC 2026-08-31 10:00 판정까지 저장했다. 복구 중 만료 신호는 신규 진입 대상으로 만들지 않는다.
- UTC 10:02 기준 signal/execute/close 상태 모두 ok=true. 현재 주기 신규 주문 0건. 사용자 설정 A/B enabled=true, test_mode=false 유지.
- 새 조합은 기본/보조 동시 보유 금지, 확인되지 않은 예약 보존, 5분 미만 신호, 불리한 이동 0.35%, 종료시각 연장 금지, 수량 고정, 가용 담보 비례 배분 적용.
- A 매매/담보 코드의 기존 동결 해시는 그대로다. B 실거래 기준 $150, 연구 $100은 별개다.
- 화면: 8종목의 기본/보조 분리, 5년 복리 +1,717,426.82%, 736회, 승률 56.93%, 종료 기준 최대낙폭 -47.28%, 시간봉 평가 -55.36%. 원천/비용/실거래 성과 아님을 표시한다.

## 남은 외부 설정/승인

1. **B 계정 ALGO/ETH/VET 롱·숏이 격리 20배다. 채택 기준 3배와 불일치.** 기존 5종목은 일치한다. 20배 그대로 거래하도록 허용하지 않았다. 각 주문 전 검증에서 불일치 주문은 차단한다. A 계정 ETH 10배를 바꾸라는 뜻이 아니다. `plan-b-stage26-preflight`는 조회/신호 복구만 가능하며 거래소 주문·설정 쓰기 권한이 없다.
2. **텔레그램 코드 수정은 완료했으나 배포는 승인 검사에서 거절됐다.** 고정 수신 채팅 `6818439075`에 A/B 잔고·평가금·포지션·거래 기록 등의 금융정보를 전송하는 기능이 있으므로 구체적 목적지·전송 데이터에 대한 명시적 승인이 필요하다. 우회 배포하지 않았다. 기존 텔레그램 운영 함수는 변경되지 않아 기존 소개 문구가 보일 수 있다.
3. GitHub/웹 반영 상태는 이 문서 끝의 추가 기록과 커밋/푸시 결과를 확인한다. 로컬 코드 수정 자체를 웹 배포 완료로 혼동하지 않는다.

## 검증

- B/제어/알림 파서·실행 테스트 52개 + A/감시/새 신호 주기 테스트 28개 통과. 총 80개 중 새 신호 주기 5개 포함.
- 보조 신호 129,995개 유효 시간봉 대조 일치; 데이터 공백 창은 거절.
- Stage16 회계 테스트 2개, 채택 자료 테스트 3개, 48개 동결 파일 검사 통과.
- 실제 DB: 신호 원자 발행·중복 CAS·누락 판정 거절·담보 예약·반대 그룹 배제·단일 선점 검사 통과. 모두 롤백. 실제 병렬 DB 세션 부하 테스트나 실거래 체결 검증을 뜻하지 않는다.
- 보안 검사 ERROR/WARN 없음. RLS 활성/정책 없음 INFO는 서비스 전용 테이블이며 anon/authenticated 권한을 부여하지 않은 의도적 상태. 공개 기록은 기존 읽기 전용 Edge 경로를 사용한다.
- 배포 중 재개 시각이 달라 '기회 상태가 현재 시간이 아님' 검사가 전환을 한 번 거절했다. 3시간 추가 복구 후 재검사를 통과하여 전환했다. 스위치를 OFF로 바꾸지 않았다.

기계 판독 배포/헬스 근거: `B_STAGE26_ROLLOUT_20260831.json`. 연구 원본/스트레스 실패 조건은 `research/results/b_sparse_stage20/`와 기존 내용 해시 ZIP에 계속 보존한다.

## GitHub 및 백업 추가 기록

- GitHub main에 `c1cfd7f` 푸시 완료. 배포 코드와 문서 49개 변경 파일 포함. 텔레그램 소스 저장은 운영 함수 배포가 아니다.
- 푸시 직후 GitHub Pages HTTP 200이나 새 B 조합 문구는 아직 확인되지 않았다. 웹 반영 완료로 단정하지 않는다.
- 최신 로컬 연구 백업: `research/archives/b_stage26_20260831_ecdae7e218e00a36.zip`, 341개 파일, SHA256 `2e4f237fb2b3cacd656d4b6b6b37d51adb8eab537f99d423da3dc1d97ad132db`. ZIP/원시 데이터는 GitHub 업로드 대상이 아니며, GitHub에는 코드·결과·보존 목록을 저장했다.
